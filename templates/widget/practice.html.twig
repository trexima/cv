<div class="row">
    <div class="col-md offset-md-4">
        <div class="form-group">
            <button class="btn btn-block btn-primary" type="button" data-trexima-european-cv-dynamic-collection-add="#{{ form.practices.vars.id }}">
                <i class="fa fa-plus" aria-hidden="true"></i>
                Pridať prax
            </button>
        </div>
    </div>
</div>

{{ form_row(form.practices) }}
